
![[Pasted image 20260627190253.png]]

데이터 흐름 
postgreSQL (Pedal_link DB)
			↓
cycleways 테이블 (2673개 LinsString)
			 ↓
MapPage.js fetch() 수신
			 ↓
Leaflet이 Geojson 위도/경도 좌표데이터 읽어서 지도에 선으로 그림


회원 가입 테이블 생성 
id BIGSERIAL PRIMARY KEY, -- ← PK는 이거. 자동 증가 숫자. login_id VARCHAR(50) NOT NULL UNIQUE, -- 사용자가 입력하는 아이디 password_hash VARCHAR(255) NOT NULL, name VARCHAR(50) NOT NULL, email VARCHAR(255) NOT NULL UNIQUE, created_at TIMESTAMP NOT NULL DEFAULT now()


## 백엔드 실행

터미널에서 `my-app-backend` 폴더로 이동 후:

```bash
cd c:\Users\kimHosung\projects\my-app-backend
./gradlew bootRun
```

→ `http://localhost:8080` 에서 실행됨

---

## 프론트엔드 실행

터미널에서 `my-app-front` 폴더로 이동 후:

```bash
cd c:\Users\kimHosung\projects\my-app-front
npm start
```

→ `http://localhost:3000` 에서 실행됨




