# 09-04 실행 복구 + DB 정리 기록

> 작업 **시작** 시점: `main` / `e7d5b16` (Merge remote-tracking branch 'origin/sjw')
> 작업 **종료** 시점: `khs` / `2b281be` — 작업 후반에 git 이력을 되돌렸다. **9번 항목 참고.**
> **이 문서의 작업은 전부 커밋되지 않은 로컬 변경분이다.**

---

## 1. 이번에 한 일 요약

1. **앱이 아예 안 뜨던 원인 제거** — `@stomp/stompjs` 미설치로 프론트 전체가 백지였다
2. **파티 API 500 복구** — `party_members.ready` 컬럼이 DB에 없었다 (002 마이그레이션 미적용)
3. **DB 죽은 스키마 정리** — 003 마이그레이션 작성·적용
4. **덤프 파일 정리** — 헷갈리던 구버전 덤프 제거, 새 덤프 생성 + 복원 검증
5. 계획표(Workplan) 기준 **DB 방향성 보고**, 둘러보기 **댓글·공유 설계** (구현 전)

---

## 2. 발생했던 오류와 원인

### 2-1. `npm start` 실패 — 포트 점유

```
error when starting dev server:
Error: Port 3000 is already in use
```

`vite.config.mjs` 의 `strictPort: true` 때문에 다른 포트로 넘어가지 않고 실패한다.
**의도된 동작이다** — 백엔드 CORS 허용 목록이 `localhost:3000` 하드코딩이라 포트가 밀리면
API가 전부 막히기 때문. 기존 dev 서버를 끄고 다시 띄우면 된다.

### 2-2. `@stomp/stompjs` 미설치 — 앱 전체 백지 (핵심)

```
Rolldown failed to resolve import "@stomp/stompjs" from "src/hooks/useChat.js"
```

- `package.json` · `package-lock.json` 에는 있는데 `node_modules` 에만 없었다
- 채팅 커밋(`684a66f`)을 `git pull` 한 뒤 **`npm install` 을 안 돌린 상태**
- `useChat.js` 를 쓰는 **ChatPage · PartyDock · PartyRoom · RidingRoom** 이 전부 모듈 로드 시점에 터지고,
  `App.jsx` 가 이들을 정적 import 하므로 **앱 전체가 백지**가 된다

**조치**: `npm install` (1개 패키지 설치)

| 검사 | 이전 | 이후 |
|---|---|---|
| `npm run build` | ✗ 실패 | ✓ 191ms / 475 모듈 |
| `npx vitest run` | ✗ 0 tests | ✓ 1 passed |

> 교훈: `git pull` 후 `npm install` 은 선택이 아니다. `cloudflared-tunnel-demo.md` 에도
> 적혀 있는 내용인데 실제로 밟았다.

### 2-3. `party_members.ready` 컬럼 누락 — `/api/parties` 500

백엔드 기동 로그에 이렇게 찍혀 있었다.

```
Error executing DDL "alter table if exists party_members add column ready boolean not null"
Caused by: PSQLException: column "ready" of relation "party_members" contains null values
```

**원인**: `party_members` 에 기존 8행이 있는데 Hibernate `ddl-auto=update` 가
`ADD COLUMN ready boolean NOT NULL` 을 **DEFAULT 없이** 생성한다.
PostgreSQL 은 기존 행을 채울 값이 없어 이 DDL 을 거부하고, 컬럼은 만들어지지 않는다.

**가장 위험한 점**: 백엔드가 이 실패를 **WARN 으로만 남기고 정상 기동한다.**
`Started MyAppBackendApplication` 만 보면 멀쩡해 보인다.

**조치**: `DB/migrations/002_party_ready_status.sql` 적용 (`DEFAULT FALSE` 포함) → 500 → 200

> ⚠️ **002 파일의 주석이 틀렸다.**
> *"이 스크립트를 실행하지 않아도 ddl-auto=update 가 컬럼을 알아서 추가해준다"* — **거짓이다.**
> 기존 행이 하나라도 있으면 반드시 실패한다. 문서 최신화 때 이 주석부터 고칠 것.

---

## 3. DB 정리 (003 마이그레이션)

작업 전 전체 덤프 백업: `DB/backup-before-003-202609040120.sql`

### 제거한 것

| 대상 | 근거 |
|---|---|
| `rider_records` 테이블 | 0행 / 자바 엔티티·리포지토리·코드 참조 **전무** / `ride_records` 의 구버전 |
| `users.login_id` | 8명 전원 NULL. User 엔티티에 없는 컬럼 |
| `users.password_hash` | 8명 전원 NULL. 현행 인증은 `users.password` (BCrypt) |
| `routes` id=1 | `user_id` 가 NULL 이라 아무에게도 조회되지 않던 테스트 행 (`test0`) |

`users_login_id_key` 유니크 인덱스는 컬럼과 함께 자동으로 사라졌다.
이 죽은 컬럼들이 `cloudflared-tunnel-demo.md` 에 적힌 **"회원가입 시 500"** 의 원인이었다.

### 보존한 것 — 지우지 않고 참조만 끊음

`ride_records` id=2, 3 은 `party_id=3` 을 가리키는데 그 파티가 이미 삭제된 상태였다.
처음엔 "고아 데이터"로 묶었지만 실제로 열어보니 **정상적인 주행 기록**이었다.

```
id=2  15.2km / 52분  user_id=14   (사용자 현존)
id=3  14.8km / 55분  user_id=13   (사용자 현존)
```

`RideRecord` 엔티티 주석대로 **`party_id = NULL` 은 오류가 아니라 "혼자 탄 기록"이라는 정상 상태**다.
그래서 행을 지우지 않고 `party_id` 만 NULL 로 되돌렸다. 주행 이력은 그대로 살아 있다.

### 검증 결과

| 항목 | 결과 |
|---|---|
| `rider_records` / 죽은 컬럼 2개 / `users_login_id_key` | 0 ✓ |
| 소유자 NULL 경로 · 깨진 파티 참조 | 0 ✓ |
| `ride_records` / `users` / `routes` | 3 / 8 / 6 (데이터 손실 없음) ✓ |
| API 5종 (`feed`·`parties`·`routes`·`ride-records`·`cycleways`) | 전부 200 ✓ |

---

## 4. 덤프 파일 정리

### 삭제 — `NEW_Pedal_link/DB/dump-Pedal_link-202608211040.sql`

헷갈림의 진짜 원인이었다.

| 항목 | 실태 |
|---|---|
| 실제 형식 | **CUSTOM (gzip 바이너리)** — `.sql` 확장자인데 텍스트 SQL 이 아님 |
| 복원 방법 | `psql -f` **불가**, `pg_restore` 필요 |
| 생성 시각 | 2026-08-21 10:40 — 추적 중이던 덤프보다 **74분 더 오래됨** |
| git 추적 / 문서 참조 | 안 됨 / **0건** |

확장자만 보고 `psql -f` 로 복원하려다 "깨진 파일"로 오해하기 딱 좋은 상태였다.
빈 `NEW_Pedal_link/` 폴더도 함께 삭제.

### 교체 — `dump-Pedal_link-202608211154.sql` → `dump-Pedal_link-202609040128.sql`

구 덤프에는 **`chat_messages` 테이블이 없어서** 복원 후 001 마이그레이션을 손으로 또 돌려야 했다.
새 덤프는 003 까지 반영돼 있어 **복원 한 번으로 끝난다.**

임시 DB 를 만들어 실제로 복원해 검증했다.

```
복원 에러 0건 / 8개 테이블 / cycleways 2673행
rider_records 없음 ✓  login_id·password_hash 없음 ✓  party_members.ready 있음 ✓
```

구 덤프는 `git rm --cached` 로 제거했으므로 **git 이력에는 남아 있다.**

### 문서 3곳 수정

| 파일 | 내용 |
|---|---|
| `cloudflared-tunnel-demo.md` | 덤프 파일명 교체 + `ddl-auto=update` 경고 추가 (2-3 사고 근거) |
| `0_v/.../05_JPA_Hibernate.md` | 존재하지 않는 `202608191557` 덤프를 가리키고 있었음 |
| `0_v/.../06_PostgreSQL_PostGIS.md` | 위와 동일 |

`페달링크_현재상항_기록/현재사항/08-19.md:150` 은 *"…로 교체했다"* 라는 **당시 작업 기록**이라
소급 수정하지 않았다. 과거 로그를 고치면 이력이 왜곡된다.

---

## 5. 현재 상태

### 실행

| 구분 | 주소 | 상태 |
|---|---|---|
| 프론트 | http://localhost:3000 | ✓ Vite 8.2.2 / ready 172ms |
| 백엔드 | http://localhost:8080 | ✓ Started in 3.587s |
| DB | localhost:5432 `Pedal_link` | ✓ PostgreSQL 18.4 + PostGIS |

`JAVA_HOME` 은 `C:\Program Files\Java\jdk-21.0.12.1` 로 올바르게 잡혀 있다
(시스템 기본 java 는 26이지만 `JAVA_HOME` 이 21을 가리켜 정상 동작).
08-19 기록에 있던 JAVA_HOME 문제는 **해결된 상태**다.

> ⚠️ 이 표는 `main` / `e7d5b16` 기준이다. 9번에서 브랜치를 옮긴 뒤로 **소스는 `khs` / `2b281be`**
> 이지만, 실행 중인 백엔드 JVM 은 옮기기 전 코드로 이미 떠 있어서 그대로 200 을 준다.
> 백엔드를 재시작하면 채팅 관련 빈이 사라진 상태로 다시 뜬다.

### DB 폴더

9번의 브랜치 이동 **이후** 실제 내용이다.

```
dump-Pedal_link-202609040128.sql    978K  ← 최신 (문서 3곳이 가리킴)
backup-before-003-202609040120.sql  983K  ← 003 롤백용. 새 덤프에 반영됐으므로 역할 종료
migrations/003_cleanup_dead_schema.sql    ← 이번 작업 (untracked 라 살아남음)
```

> **001·002 마이그레이션 파일이 작업트리에서 사라졌다.** 두 파일은 커밋에 포함돼 있었고
> `khs`(2b281be) 는 그 커밋들보다 앞선 시점이라 체크아웃되지 않는다.
> **DB 에는 두 마이그레이션이 이미 적용돼 있으므로 다시 실행할 필요는 없다.**
> `main` 으로 돌아가면 두 파일도 함께 돌아온다.

### 커밋 안 된 변경분

```
M  0_v/페달링크 자료 설명/05_JPA_Hibernate.md
M  0_v/페달링크 자료 설명/06_PostgreSQL_PostGIS.md
D  DB/dump-Pedal_link-202608211154.sql
R  페달링크_현재상항_기록/0627데이터베이스 테이블 생성.md
     -> 페달링크_현재상항_기록/현재사항/06-27 데이터베이스 테이블 생성.md
?? DB/dump-Pedal_link-202609040128.sql
?? DB/backup-before-003-202609040120.sql
?? DB/migrations/003_cleanup_dead_schema.sql
?? cloudflared-tunnel-demo.md
?? 페달링크_현재상항_기록/README.md
?? 페달링크_현재상항_기록/현재사항/09-04.md
```

브랜치를 두 번 옮겼는데도 위 목록이 그대로 유지됐다. 수정한 파일들이 `main`·`khs`
양쪽에서 내용이 같아서 git 이 충돌 없이 들고 다닐 수 있었기 때문이다.

---

## 6. 아직 남은 문제 (미수정)

우선순위 순.

### 6-1. 파티 참가 수락·거절에 호스트 검사가 없다

```
POST /api/parties/{id}/requests/{userId}/approve
POST /api/parties/{id}/requests/{userId}/reject
```

**호스트 userId 를 받지도 않고** `PartyService.approve` 에도 검사가 없다.
→ **아무나 남의 파티 신청을 수락·거절할 수 있다.**
`delete` · `start-ride` · `end` 에는 403 검사가 있는데 이 둘만 빠졌다.

### 6-2. 경로 삭제 API 에 소유자 검사가 없다

```java
@PostMapping("/routes/delete")
public ResponseEntity<?> deleteRoutes(@RequestBody List<Long> ids)
```

`userId` 를 **받지도 않는** 구조라 소유자 검사가 불가능하다.
routeId 만 알면 누구나 남의 경로를 지운다.
→ **공유 기능을 붙이면 routeId 가 외부로 퍼지므로 실제 위험이 된다.**

### 6-3. 피드가 미공개 경로까지 노출한다

`FeedService.getFeed` 가 `findAllByOrderByCreatedAtDesc()` — 조건 없이 전체를 가져온다.

```
피드에 내려오는 카드: 6장
그중 실제로 "게시물 올리기"를 한 것: 4장
```

**2장은 지도에서 저장만 하고 공개한 적 없는 경로**인데 남들 피드에 떠 있다.
지금은 링크가 없어 티가 안 나지만 공유 기능이 붙으면 문제가 커진다.

### 6-4. `chat_messages` 에 `room_id` 인덱스가 없다

`findByRoomIdOrderByCreatedAtAsc` 가 풀스캔이다. 현재 0행이라 체감되지 않지만
**계획표 2~3번(채팅) 착수 시 바로 병목**이 된다. `(room_id, created_at)` 복합 인덱스 하나면 된다.

### 6-5. 기타

| 항목 | 내용 |
|---|---|
| `build.gradle` | `starter-web`·`data-jpa`·`postgresql`·`lombok` 이 **두 번씩** 선언됨 |
| `npm audit` | undici 7.0.0–7.28.0 high 5건 (Vite 전이 의존성, 런타임 아님) |
| 번들 크기 | `index.js` 620KB / `backglound1.png` **7.87MB** — 배경 이미지가 번들 대부분 |
| 카카오 키 | `.env.local` 의 `VITE_KAKAO_API_KEY` 주석 처리 → 지도 **장소 검색만** 비활성 |
| `websocket-chat-plan.md` | 계획 문서와 실제 구현이 다름 (SockJS 우회, `ChatMessage` 2분할, highlight.js 미설치) |
| `SETUP.md` | CRA 시절 기준. Vite 전환·migrations·로컬 설정파일 2개 안내 누락 |

---

## 7. 다음 작업 — 둘러보기 댓글 · 공유 (설계 완료, 구현 전)

계획표 5번 "댓글·좋아요 백 엔드 / 데이터구현" 에 해당한다.
`BrowsePage.jsx` 의 댓글·공유·저장 버튼은 현재 `disabled` 상태이고 아이콘·CSS 는 이미 있다.

### 댓글

`route_likes` 와 동일한 패턴으로 `comments` 테이블 신설.

```sql
CREATE TABLE comments (
  id BIGSERIAL PRIMARY KEY,
  route_id BIGINT NOT NULL,
  user_id BIGINT NOT NULL,
  content VARCHAR(300) NOT NULL,
  created_at TIMESTAMP NOT NULL DEFAULT now()
);
CREATE INDEX idx_comments_route ON comments (route_id, created_at);
```

- 대댓글(`parent_id`)은 넣지 않는다 — 계획에 없고 나중에 컬럼 하나 추가하면 된다
- 댓글 수 집계는 `RouteLikeRepository.countGroupedByRouteId()` 처럼 **GROUP BY 한 방**으로 (N+1 회피)
- UI 는 카드 하단 인라인 펼침 + 첫 열람 시 지연 로드

### 공유 · 저장

**공유 = 링크를 밖으로 / 저장 = 그 경로를 내 히스토리로 복사** 로 역할을 나눈다.

경로 지오메트리 실측 결과 **82 bytes ~ 8.2 KB (평균 약 3KB)** 로 작아서 **복사 방식**을 택한다.
참조 방식은 원본이 삭제되면 담아둔 사람들의 히스토리가 전부 증발하는데,
6-2 취약점 때문에 **누구나 원본을 지울 수 있어** 위험이 크다.

```sql
ALTER TABLE routes ADD COLUMN IF NOT EXISTS source_route_id BIGINT;
ALTER TABLE routes ADD COLUMN IF NOT EXISTS source_user_id  BIGINT;
```

```
POST /api/routes/{routeId}/save  { userId }  →  { newRouteId }
```

- 복사함: 경로명·출발·도착·지오메트리 2종·거리·상승고도·시간
- 복사 안 함: `description`, `tags` (남의 게시물 문구가 딸려오면 안 됨), `id`, `user_id`, `created_at`
- 막을 것: 자기 경로 담기 / 중복 담기 / 원본 삭제 후 담기
- 공유 링크는 `/browse?routeId=9` — `App.jsx` 의 `/map?partyId=` 파싱 패턴 재사용 (백엔드 변경 0)
- `navigator.share` → `clipboard` → 입력창 **3단 폴백 필수**
  (둘 다 HTTPS 보안 컨텍스트 전용. `http://내부IP:3000` 에서는 조용히 실패한다)

---

## 8. 계획표 기준 DB 방향성 (요약)

| 계획표 | DB 작업 |
|---|---|
| 2~3 채팅·이미지 첨부 | **`chat_rooms` 신설** — 지금 `room_id` = `partyId` 라 4번에서 동호회 채팅이 오면 방이 충돌한다. 첨부는 URL 만 저장 (파일은 8번 S3) |
| 4 동호회·정기링크·가입 | `clubs`, `club_members`, `parties.club_id` — `party_members` 구조 그대로 재사용 |
| 5 댓글·좋아요·지역 | `comments`, `users.region`. 피드가 느려지므로 `like_count`·`comment_count` 비정규화 캐시 검토 |
| 6 DB 구현화 안정 | **여기가 승부처** — Flyway 도입(`ddl-auto=validate`), 인덱스·FK 일괄 추가, 고아 정리 |
| 7 QA 실주행 | 실시간 위치는 `LocationController` 의 `ConcurrentHashMap` (DB 미사용, 15초 TTL) |
| 8 AWS 이전 | PostGIS 확장 필수. 데이터는 약 8MB 로 가벼움. **다중 인스턴스면 위치 공유가 깨진다** (Redis 필요) |

> 현재 스키마 관리 방식(`ddl-auto=update` + 손으로 돌리는 SQL)이 가장 큰 리스크다.
> 2-3 사고가 바로 이 방식의 실패였고, 8번에 "데이터베이스 마이그레이션"이 명시된 이상
> 그 전에 Flyway 로 옮겨야 한다.

---

## 9. git 이력 되돌리기 + 브랜치 이동

위 작업을 마친 뒤 진행했다. **코드는 뒤로 갔지만 DB 와 문서 작업은 그대로 남아 있다.**

### 9-1. `main` 을 `4880d27` 로 되돌림

`4880d27` 은 **`origin/main` 그 자체**였다. 되돌린 결과 로컬 `main` 이 원격과 정확히 일치한다.

빠진 커밋은 **3개**다 (커밋 그래프상 2개로 보이지만 `4880d27..HEAD` 는 3개를 반환한다).

| 커밋 | 내용 | 원격 보존 |
|---|---|---|
| `e7d5b16` | Merge origin/sjw | **로컬 전용** — 백업 브랜치에만 있음 |
| `092c803` | 협동 라이딩 룸(RidingRoom) | ✓ `origin/sjw` |
| `580fc52` | 파티 룸 오버레이 + 준비 상태 | ✓ `origin/sjw` |

코드 변경은 **16개 파일 / +1876 −34 줄**. `RidingRoom.jsx` · `PartyRoom.jsx` ·
`ridingroom.css` · `partyroom.css` 삭제, `PartyDock` · `MapPage` · `PartyService` ·
`PartyController` 원복, `PartyMember.ready` 필드 제거, **`002_party_ready_status.sql` 삭제.**

**절차 — 오늘 작업을 지키기 위해 3단계로 했다.**

```bash
git branch backup/pre-reset-20260904-0221 HEAD   # 1) 되돌리기 전 지점 보존
git stash push -m "..."                          # 2) 추적 중인 변경분 보관
git reset --hard 4880d27
git stash pop --index                            # 3) staged 상태까지 그대로 복원
```

`reset --hard` 는 **추적 중인 파일만** 되돌린다. untracked 파일(003 마이그레이션 · 새 덤프 ·
이 기록 등)은 원래 영향을 안 받지만, 문서 수정 2건 · 덤프 삭제 · 노트 이동은 tracked 라
stash 없이 했으면 **전부 날아갔다.**

### 9-2. GitHub pull — 가져올 것 없음

```
Already up to date.
```

`git ls-remote` 로 확인한 GitHub 실제 상태:

| 브랜치 | 커밋 | 비고 |
|---|---|---|
| `main` | `4880d27` | 되돌린 지점과 동일 → pull 이 no-op |
| `sjw` | `092c803` | 9-1 에서 뺀 RidingRoom 커밋이 **여기 살아 있다** |
| `khs` | `bc556c2` | WebSocket 채팅 서버 |
| `ys` | `533d2f2` | 플로팅 버튼 |

### 9-3. `khs` 브랜치로 이동

```
khs / 2b281be (feat: 파티 함께 달리기 + 경로 접근 권한/예외 처리 수정)
origin/khs 보다 7커밋 뒤처짐
```

**`khs` 는 고유 작업이 하나도 없는 낡은 포인터다.**

| 비교 | 결과 |
|---|---|
| `main` 에 있고 로컬 `khs` 에 없는 커밋 | **10개** |
| 로컬 `khs` 에 있고 `main` 에 없는 커밋 | **0개** |
| `origin/khs` 가 `main` 의 조상인가 | **예** — 원격 khs 도 main 에 이미 포함 |

이 시점에는 **채팅 기능이 통째로 없다.**

```
useChat.js · ChatPage.jsx · api/chat.js           없음
WebSocketConfig.java · ChatController.java        없음
DB/migrations/001_chat_messages.sql               없음
package.json 의 @stomp/stompjs                    없음
```

2-2 에서 고쳤던 그 채팅 기능이, 이 커밋 시점에는 애초에 존재하지 않는다.

### 9-4. ⚠️ DB 가 코드보다 앞서 있다 — 가장 주의할 점

되돌린 건 **코드뿐**이고 DB 는 오늘 작업한 상태 그대로다.

| DB 객체 | DB | 코드(2b281be) |
|---|---|---|
| `chat_messages` 테이블 | **있음** | 쓰는 코드 없음 |
| `party_members.ready` 컬럼 | **있음** | `PartyMember.ready` 필드 없음 |
| `rider_records` 테이블 | 없음 (003 으로 삭제) | — |

**당장은 문제가 안 된다.** JPA 는 엔티티에 없는 여분 컬럼·테이블을 무시하므로
`/api/parties` 는 정상 200 이다. 되돌린 직후에도 확인했다.

문제가 되는 건 이 두 경우다.

- **덤프를 복원해 DB 를 다시 만들 때** — 새 덤프(`202609040128`)에는 `chat_messages` 와
  `ready` 컬럼이 들어 있는데, 그 시점 코드에는 대응 엔티티가 없다. 그래도 동작은 한다.
- **`ddl-auto` 를 `validate` 로 바꿀 때** — 그때는 엔티티와 DB 가 정확히 일치해야 해서
  이 불일치가 곧바로 기동 실패로 나타난다. 8번 "Flyway 도입" 항목과 직결된다.

### 9-5. 되돌리는 방법

**뺀 커밋 전체 복구**

```bash
git switch main
git reset --hard backup/pre-reset-20260904-0221
```

**RidingRoom · PartyRoom 만 다시 가져오기** — `origin/sjw` 에 남아 있다

```bash
git switch main
git merge origin/sjw
```

`e7d5b16` 이 정확히 이 머지였으므로 같은 결과가 된다. `002_party_ready_status.sql` 도 함께
돌아오는데 **DB 에는 이미 적용돼 있어 재실행할 필요가 없다** (그 파일은 `IF NOT EXISTS` 라
실행해도 무해하다).

**`khs` 를 원격 최신으로** — 7커밋 fast-forward, 채팅 기능이 돌아온다

```bash
git pull
```

> 백업 브랜치 `backup/pre-reset-20260904-0221` 은 남겨뒀다.
> `origin/sjw` 로 병합을 마쳐 더 필요 없어지면 `git branch -D` 로 지우면 된다.
