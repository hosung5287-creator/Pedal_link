

- **`<option value="인천">` 먼저** — 사용자가 "인천"을 못 고르게 막는 입구. 이걸 먼저 지우면 호출 자체가 불가능해진다.

- **`if (val === '인천') loadIncheonCycleway()` 호출부** — 이제 아무도 안 부르는 죽은 분기. 제거.

- **`loadIncheonCycleway` 함수 전체 (342~376)** — 호출하는 데가 없어졌으니 안전하게 통삭제.

- **`incheonLayer` state (16번 줄)** — 마지막. 이 state를 쓰는 게 위 함수밖에 없었다면 같이 제거.