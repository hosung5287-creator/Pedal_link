.claude/
.agents/
.codex/
.cursor/
.windsurf/
AGENTS.md
GEMINI.md

# Superpowers SDD 스크래치(진행 원장 등)
.superpowers/

# Eclipse IDE 워크스페이스 캐시
.metadata/

# Eclipse 프로젝트 로컬 설정
.settings/
.project
.classpath
bin/

# Node (의존성 — 커밋 금지)
node_modules/
**/node_modules/

# Gradle
.gradle/
build/

# Obsidian 로컬 UI 상태 (나머지 .obsidian 설정은 공유)
# ** 로 시작해야 하위 폴더의 보관함(0_v 등)까지 걸린다
**/.obsidian/workspace.json

# OS
.DS_Store
Thumbs.db

# 크롬/크래시패드 등이 남기는 로그 (앱과 무관)
debug.log

# 환경변수 (API 키가 들어간다 — .bak 등 사본까지 막는다)
.env.local
.env.local.*
**/.env.local
**/.env.local.*

# 작업표
docs

# 개인 참고 문서 (프로젝트 구조 지도)
PROJECT_MAP.md

# Local Remotion project, source recordings, and generated media
/pedal-link-motion/
/output/
